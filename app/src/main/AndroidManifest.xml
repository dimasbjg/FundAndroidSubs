<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fundandroidsubs">

    <permission android:name="com.example.fundandroidsubs.READ_DATABASE" android:protectionLevel="normal" />
    <permission android:name="com.example.fundandroidsubs.WRITE_DATABASE" android:protectionLevel="normal" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FundAndroidSubs">
        <provider
            android:authorities="com.example.fundandroidsubs"
            android:name=".provider.UserProvider"
            android:exported="true"
            android:enabled="true"
            android:readPermission="com.example.fundandroidsubs.READ_DATABASE"
            android:writePermission="com.example.fundandroidsubs.WRITE_DATABASE" />

        <activity
            android:name=".FavoritesListActivity"
            android:parentActivityName=".MainActivity"
            android:noHistory="true"/>
        <activity
            android:name=".DetailUserActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>